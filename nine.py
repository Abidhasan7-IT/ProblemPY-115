# String Concatenation: Write a Python program that takes two strings 
# as input and concatenates them into a single string without using the + operator.

str1= str(input("Enter First String: "))
str2= str(input("Enter Second String: "))

add= f"{str1} {str2}"

print("Concatenated String is: ", add)
